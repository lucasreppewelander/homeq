import React from 'react';
import { shallow, mount, render } from 'enzyme';

import {detached as Application} from '../src/Application';

xdescribe('Application', () => {
	// application specific tests here
	xit('special test', () => {})
});