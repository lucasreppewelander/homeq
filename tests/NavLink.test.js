import React from 'react';
import { shallow, mount, render } from 'enzyme';

import {NavLink} from '../src/components/Header/Navigation';

xdescribe('NavLink', () => {
	// navlink specific tests here
	xit('special test', () => {})
});